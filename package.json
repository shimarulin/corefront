{
  "name": "corefront",
  "description": "",
  "license": "ISC",
  "private": true,
  "author": "Vyacheslav Shimarulin <shimarulin@gmail.com>",
  "scripts": {
    "fmt": "pnpm run fmt:pkgs",
    "fmt:pkgs": "syncpack format",
    "pkgs:up": "pnpm syncpack fix-mismatches && pnpm update --recursive --latest",
    "pkgs:up:i": "pnpm syncpack fix-mismatches && pnpm update --recursive --interactive --latest"
  },
  "devDependencies": {
    "syncpack": "^12.3.0"
  }
}
